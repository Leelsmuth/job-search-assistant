# Discovery source slug lists

Slug lists harvested from public Common Crawl ATS URL indexes via [Feashliaa/job-board-aggregator](https://github.com/Feashliaa/job-board-aggregator) (MIT). These are **candidates only** — each slug must pass live API verification before counting toward the registry target.

| File | Provider | Approx. slugs |
|------|----------|---------------|
| `greenhouse_slugs.json` | Greenhouse | ~8,300 |
| `ashby_slugs.json` | Ashby | ~3,160 |
| `lever_slugs.json` | Lever | ~4,370 |

Refresh: download latest from the upstream repo's `data/` folder, or replace with your own discovery output.
